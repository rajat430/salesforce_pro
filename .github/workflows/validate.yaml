name: build_workflow
on:
  pull_request:
    branches: [ dev,release,uat,preprod,main ]
    types: [ opened, synchronize, reopened ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: validate_pr
        run: |
          echo "Validating pull request..."
          # Add your validation commands here